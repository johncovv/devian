import { Router } from 'express';

// import UserController from '../controllers/UserController';

const routes = Router();

// routes.get('/auth/redirect', UserController.auth);

export default routes;
